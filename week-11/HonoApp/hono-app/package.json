{
  "name": "hono-app",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@types/node": "^24.0.4",
    "hono": "^4.8.3"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
