import React from 'react'

const BottomWarning = ({ lable, button }) => {
    return (
        <div>
            {lable}
            <button>
                {button}
            </button>
        </div>
    )
}

export default BottomWarning